<Window x:Class="Files_Transfer.MessageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:Files_Transfer"
        mc:Ignorable="d"
        Title="MessageWindow" Height="180" Width="360"
        WindowStartupLocation="CenterOwner" WindowStyle="None"
        MouseLeftButtonDown="Drag" Background="Transparent"
        ResizeMode="CanMinimize">
    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="-1"></WindowChrome>
    </WindowChrome.WindowChrome>
    <Window.Template>
        <ControlTemplate TargetType="Window">
            <Grid>
                <Border Background="{DynamicResource MessageWindowBackgroundBrush}" CornerRadius="10">
                    <ContentPresenter></ContentPresenter>
                </Border>
            </Grid>
        </ControlTemplate>
    </Window.Template>
    <Window.Style>
        <Style TargetType="Window">
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <TranslateTransform X="0" Y="0"></TranslateTransform>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                             From="0" To="1" Duration="0:0:0.2"></DoubleAnimation>
                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                             From="30" To="0" Duration="0:0:0.5" DecelerationRatio="0.6">
                                <DoubleAnimation.EasingFunction>
                                    <BackEase Amplitude="1" EasingMode="EaseOut"></BackEase>
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>
    </Window.Style>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" VerticalContentAlignment="Center" HorizontalContentAlignment="Left"
               FontSize="25" Foreground="{DynamicResource MessageWindowForegroundBrush}"
               Margin="20 0" Name="TitleLabel"></Label>
        <Label Grid.Row="1" FontSize="15" Foreground="{DynamicResource MessageWindowForegroundBrush}"
               VerticalContentAlignment="Center" HorizontalContentAlignment="Left" Margin="20 0" Name="Message"></Label>
        <Button Grid.Row="2" Style="{StaticResource SimpleButton}" BorderBrush="{DynamicResource MessageWindowButtonBrush}"
                Foreground="{DynamicResource MessageWindowButtonBrush}" Content="关闭" FontSize="15"
                HorizontalAlignment="Right" Margin="20 0" Click="CloseButton" Width="80" Height="30"></Button>
    </Grid>
</Window>
